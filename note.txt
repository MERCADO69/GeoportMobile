* maps dislay
    -mapbox
* routing algo
    - open route service
* rerouting algo
    - mapbox api
* reverse geocoding
    - locationIQ api
* database
    - realtime database (storage)
    - firebase auth (authentication and account verification)
* audio conferencing (call)
    - livekit (cloud)
* face verification and extraction
    - compreface
* request limiter (backend)
    - fastapi limiter w/ redis


# To generate cloudflare url 
- cloudflared tunnel --url http://localhost:8000 --protocol http2

